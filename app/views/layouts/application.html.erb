<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Cursos Online" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Cursos Online" %>">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'partials/navigation' %>
    </header>
    <main role="main">
       <%= render 'partials/messages' %>
       <div class="container">
         <%= yield %>
       </div>

       <!-- Navegacion -->
         <%= breadcrumbs  %>
       <!-- /Navegacion -->
    </main>

  <!-- Pie -->
  <hr>
  <footer class="bottom">
    <div class="container hidden-xs">
     <ul class="nav navbar-nav pull-left">
       <li><a href="#" class="fa fa-rocket"> Cursos - <time datetime="2018-04-01">2018</time></a></li>
       <li><%= link_to ' Acerca de', pages_about_path, class: "fa fa-info-circle" %></li>
       <li><%= link_to ' Contacto',  pages_contact_path, class: "fa fa-barcode" %></li>
     </ul>
   </div>

   <div class="container visible-xs">
     <p class="navbar-text pull-left"> Cursos - <time datetime="2018-04-01">2018</time></p>
     <a href="#" class="navbar-btn btn-info btn pull-right">Cursos</a>
   </div>
  </footer>
  </body>
</html>
